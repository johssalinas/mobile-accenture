# Variables de entorno para desarrollo local
# Copiar este archivo a .env y configurar los valores reales
# El script scripts/set-env.js leerá estas variables

# Firebase Configuration
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_PROJECT_ID=
FIREBASE_STORAGE_BUCKET=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_APP_ID=

# AI Proxy URL (AWS Lambda API Gateway)
# Obtener con: cd terraform && terraform output suggest_endpoint
# Dejar vacío para usar mock local
AI_PROXY_URL=
